#!/usr/bin/env bash
# Example hook (POST) — runs AFTER all modules.
# Activate: copy the file to hooks/post.d/<NN>-name.sh (suffix .sh, without .example).
set -euo pipefail

echo "[post-hook] Example: insert your own follow-up work here."
