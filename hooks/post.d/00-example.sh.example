#!/usr/bin/env bash
# Beispiel-Hook (POST) — läuft NACH allen Modulen.
# Aktivieren: Datei nach hooks/post.d/<NN>-name.sh kopieren (Endung .sh, ohne .example).
set -euo pipefail

echo "[post-hook] Beispiel: hier eigene Nacharbeiten einfügen."
